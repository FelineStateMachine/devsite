/target
/web/pkg

# Secrets. The capability signing key is the root of trust for every daemon, and identity
# keys are daemon identities — none of it belongs in the repository.
/.devsite-state
/data
*.key
*.pem

# Local state
*.db
*.db-journal
/.e2e

# Generated: contains the tunnel id and a path to account credentials.
deploy/cloudflared.yml

# Tooling scratch
.playwright-cli/
output/
